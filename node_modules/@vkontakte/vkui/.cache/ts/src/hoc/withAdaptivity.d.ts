import { SizeType, ViewHeight, ViewWidth, AdaptivityProps } from '../components/AdaptivityProvider/AdaptivityContext';
export { SizeType, ViewWidth, ViewHeight };
export type { AdaptivityProps };
interface Config {
    sizeX?: boolean;
    sizeY?: boolean;
    viewWidth?: boolean;
    viewHeight?: boolean;
    hasMouse?: boolean;
}
export declare function withAdaptivity<T>(TargetComponent: T, config: Config): T;
